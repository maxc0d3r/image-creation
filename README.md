Scripts to create a CentOS 6 AMI with custom partitioning.

Steps:

1. Source configure.sh # This will setup required environment variables.
2. Execute create-image.sh # This script takes small and medium as arguments. 

# source configure.sh
# sh create-image.sh -t [small|medium]
